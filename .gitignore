# Virtual environment — never commit this
venv/

# Python cache files
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# Environment variables — API KEYS LIVE HERE — NEVER COMMIT
.env

# Raw data — can be re-fetched, no need to store in git
data/raw/

# Trained model — large binary file
data/model.pkl
data/*.pkl

# Mac system file
.DS_Store

# VS Code settings
.vscode/